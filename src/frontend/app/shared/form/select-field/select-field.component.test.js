import React from 'react';

import SelectFieldComponent from './select-field.compoent';

describe('>>> SelectFieldComponent', () => {
  it('+++ должен быть определен', () => {
    expect(SelectFieldComponent).toBeDefined();
  });
})