// Actions
import * as categoriesActions from './categories.actions';

describe('>>> Categories actions', () => {
  
  it('+++ should be defined addCategory function', () => {
    expect(categoriesActions.addCategory).toBeDefined();
  });
    
});
  
